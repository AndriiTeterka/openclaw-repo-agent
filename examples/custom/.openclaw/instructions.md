# Repo Agent Instructions

- This workspace is managed by `openclaw-repo-agent`.
- Use the repo's configured ACP default agent when you need ACP-backed inspection, edits, or verification.
- Keep replies concise in Telegram-style channels.
- Run the configured verification commands after relevant code changes.
- Do not commit local-only secrets from `.openclaw/local.env`.
